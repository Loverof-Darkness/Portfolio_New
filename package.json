{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Abhay Gupta — Research Scientist portfolio (static site)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.9"
  }
}

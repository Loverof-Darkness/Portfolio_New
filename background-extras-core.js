(()=>{'use strict';
const EXTRAS=[
{name:'GalaxyJS Black Hole',type:'galaxyjs-blackhole',draw:blackHole},
{name:'Interactive Falling Starfield',type:'codeshack-starfield',draw:fallingStars},
{name:'Parallax Galaxy',type:'parallax-galaxy',draw:parallaxGalaxy},
{name:'3D Hyperspace',type:'3d-starfield',draw:hyperspace}
];
const forced=Number.parseInt(window.__PORTFOLIO_FORCED_EXTRA_BACKGROUND ?? '-1',10);
const KEY='portfolio:extra-background-bag-v2';
let bag=[];try{bag=JSON.parse(localStorage.getItem(KEY)||'[]')}catch{bag=[]}
if(!Array.isArray(bag)||bag.length===0||bag.some(i=>i<0||i>=EXTRAS.length)){bag=Array.from({length:EXTRAS.length},(_,i)=>i);for(let i=bag.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[bag[i],bag[j]]=[bag[j],bag[i]]}}
let index;if(Number.isInteger(forced)&&forced>=0&&forced<EXTRAS.length){const p=bag.indexOf(forced);if(p>=0)bag.splice(p,1);index=forced}else index=bag.shift();localStorage.setItem(KEY,JSON.stringify(bag));
const mode=EXTRAS[index];
function hex(h){const m=h.replace('#','').match(/.{2}/g);return m?m.slice(0,3).map(x=>parseInt(x,16)):[255,255,255]}
function color(name,fallback){const v=getComputedStyle(document.documentElement).getPropertyValue(name).trim();return v||fallback}
function pal(){return{bg:color('--theme-bg','#05070A'),p:color('--theme-primary','#67E8F9'),s:color('--theme-secondary','#A78BFA'),a:color('--theme-tertiary','#F9A8D4'),text:color('--theme-text','#F5F8FF')}}
function rgba(c,a){const [r,g,b]=hex(c);return`rgba(${r},${g},${b},${a})`}
function setup(){const old=document.getElementById('dynamic-bg');if(!old)return;document.getElementById('dynamic-background-extra-style')?.remove();const canvas=document.createElement('canvas');canvas.id='dynamic-bg-extra';canvas.setAttribute('aria-hidden','true');canvas.style.cssText='position:fixed;inset:0;width:100vw;height:100vh;z-index:0;pointer-events:none;user-select:none;display:block';document.body.prepend(canvas);old.style.opacity='0';old.style.visibility='hidden';const style=document.createElement('style');style.id='dynamic-background-extra-style';style.textContent='#dynamic-bg-extra{opacity:.98}@media(prefers-reduced-motion:reduce){#dynamic-bg-extra{opacity:.55}}';document.head.appendChild(style);const ctx=canvas.getContext('2d',{alpha:false});if(!ctx)return;let w=0,h=0,dpr=1,lastT=performance.now(),stars=[],raf=0;const reduced=window.matchMedia('(prefers-reduced-motion: reduce)'),isReduced=()=>reduced.matches;
function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);init()}
function init(){const n=Math.min(520,Math.max(150,Math.floor(w*h/6200)));stars=Array.from({length:n},()=>({x:Math.random()*w,y:Math.random()*h,z:.08+Math.random()*.98,r:.45+Math.random()*1.6,o:.25+Math.random()*.75,a:Math.random()*Math.PI*2,s:.2+Math.random()*.9}))}
function clear(){ctx.fillStyle=pal().bg;ctx.fillRect(0,0,w,h)}
function blackHole(t){const c=pal(),cx=w*.7,cy=h*.48,R=Math.min(w,h)*.105,halo=ctx.createRadialGradient(cx,cy,R*.2,cx,cy,R*3.6);halo.addColorStop(0,rgba(c.a,.25));halo.addColorStop(.2,rgba(c.p,.12));halo.addColorStop(.5,rgba(c.s,.045));halo.addColorStop(1,rgba(c.bg,0));ctx.fillStyle=halo;ctx.fillRect(cx-R*4,cy-R*4,R*8,R*8);for(let k=0;k<5;k++){ctx.beginPath();for(let i=0;i<=240;i++){const a=i/240*Math.PI*2,rr=R*(1.35+k*.22)+Math.sin(a*3+t*.0007+k)*R*.025,x=cx+Math.cos(a)*rr*1.7,y=cy+Math.sin(a)*rr*.58;if(!i)ctx.moveTo(x,y);else ctx.lineTo(x,y)}ctx.strokeStyle=rgba(k%2?c.p:c.a,.08+k*.012);ctx.lineWidth=1.2;ctx.stroke()}const g=ctx.createRadialGradient(cx-R*.25,cy-R*.2,R*.05,cx,cy,R);g.addColorStop(0,c.bg);g.addColorStop(.48,c.bg);g.addColorStop(.62,rgba(c.text,.94));g.addColorStop(.72,rgba(c.a,.7));g.addColorStop(.86,rgba(c.p,.18));g.addColorStop(1,rgba(c.bg,0));ctx.fillStyle=g;ctx.beginPath();ctx.arc(cx,cy,R*1.75,0,Math.PI*2);ctx.fill();ctx.fillStyle='#000';ctx.beginPath();ctx.arc(cx,cy,R*.94,0,Math.PI*2);ctx.fill();for(const s of stars){if(!isReduced()){s.x+=Math.sin(s.a)*s.s*.018;s.y+=Math.cos(s.a)*s.s*.018}s.a+=.0005;if(s.x<-5)s.x=w+5;if(s.x>w+5)s.x=-5;if(s.y<-5)s.y=h+5;if(s.y>h+5)s.y=-5;ctx.fillStyle=rgba(s.z>.65?c.text:c.s,s.o*.55);ctx.fillRect(s.x,s.y,s.r,s.r)}}
function fallingStars(t,dt){const c=pal();for(const s of stars){if(!isReduced())s.y+=(25+s.z*110)*dt/1000;if(s.y>h+8){s.y=-8;s.x=Math.random()*w}s.x+=Math.sin(s.a+t*.00015)*.04;ctx.beginPath();ctx.fillStyle=rgba(c.text,.15+s.o*.45);ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill()}if(Math.floor(t/2200)%2===0){const n=Math.floor((t%2200)/180),x=n*113%w,y=t%1200/1200*h,g=ctx.createLinearGradient(x-50,y-80,x,y);g.addColorStop(0,rgba(c.p,0));g.addColorStop(1,rgba(c.text,.9));ctx.strokeStyle=g;ctx.lineWidth=1.2;ctx.beginPath();ctx.moveTo(x-50,y-80);ctx.lineTo(x,y);ctx.stroke()}}
function parallaxGalaxy(t,dt){const c=pal(),cx=w*.67,cy=h*.5,arms=5;for(let arm=0;arm<arms;arm++){for(let i=0;i<130;i++){const q=i/130,rad=35+q*q*Math.min(w,h)*.48,ang=arm/arms*Math.PI*2+q*7.2+t*.00008*(1-q),jitter=(Math.random()-.5)*(.2+q*2.4),x=cx+Math.cos(ang+jitter)*rad,y=cy+Math.sin(ang+jitter)*rad*.42;ctx.fillStyle=rgba(i%7===0?c.a:(arm%2?c.p:c.s),.06+q*.22);ctx.fillRect(x,y,1+q*1.7,1+q*1.7)}}const core=ctx.createRadialGradient(cx,cy,0,cx,cy,120);core.addColorStop(0,rgba(c.text,.34));core.addColorStop(.25,rgba(c.a,.16));core.addColorStop(1,rgba(c.bg,0));ctx.fillStyle=core;ctx.fillRect(cx-120,cy-120,240,240);for(const s of stars){if(!isReduced())s.x+=Math.sin(s.a+t*.0002)*.01;ctx.fillStyle=rgba(c.text,s.o*.22);ctx.fillRect(s.x,s.y,s.r,s.r)}}
function hyperspace(t,dt){const c=pal(),cx=w*.5,cy=h*.5;for(const s of stars){if(!isReduced())s.z-=dt*.00022*(.35+s.r);if(s.z<=.01){s.z=1;s.x=Math.random()*w;s.y=Math.random()*h}const px=(s.x-cx)/s.z+cx,py=(s.y-cy)/s.z+cy,nz=Math.max(.01,s.z+.015),px2=(s.x-cx)/nz+cx,py2=(s.y-cy)/nz+cy,g=ctx.createLinearGradient(px2,py2,px,py);g.addColorStop(0,rgba(c.p,0));g.addColorStop(1,rgba(s.r>1.3?c.text:c.s,Math.min(.8,.08+(1-s.z)*.85)));ctx.strokeStyle=g;ctx.lineWidth=.5+s.r*.5;ctx.beginPath();ctx.moveTo(px2,py2);ctx.lineTo(px,py);ctx.stroke()}ctx.beginPath();ctx.arc(cx,cy,8+Math.sin(t*.002)*2,0,Math.PI*2);ctx.fillStyle=rgba(c.a,.14);ctx.fill()}
function frame(now){const dt=Math.min(now-lastT,40);lastT=now;clear();mode.draw(now,dt);if(isReduced()){raf=0;return}raf=requestAnimationFrame(frame)}resize();addEventListener('resize',resize,{passive:true});window.portfolioBackground=Object.freeze({index:index+12,name:mode.name,type:mode.type,sourceInspired:true});if(isReduced())frame(performance.now());else raf=requestAnimationFrame(frame)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',setup,{once:true});else setup();
})();
